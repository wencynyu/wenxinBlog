{
  "name": "wenxinblog",
  "version": "0.0.1",
  "private": true,
  "description": "WenxinBlog monorepo (microservices + web + mobile)",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.9.5"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,css,scss,md,json}": "prettier --write",
    "*.go": "gofmt -w"
  }
}
